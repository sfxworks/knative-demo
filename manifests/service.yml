apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
 name: helloworld-nodejs
 namespace: default
spec:
 template:
   spec:
     containers:
     - image: docker.io/quantomworks/knative-demo
       env:
         - name: TARGET
           value: "Node.js Sample v1"
